# Teacher Model Evaluation — Requirements
# Hardware: 8× NVIDIA RTX A6000 (49 GB each) | CUDA 12.1+
#
# ── INSTALL ORDER ─────────────────────────────────────────────────────────────
# 1.  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 2.  pip install flash-attn --no-build-isolation
# 3.  ./setup_lmms_eval.sh   (or pip install -r requirements_eval.txt)

# ── lmms-eval (install from source, see setup_lmms_eval.sh) ──────────────────
# git clone https://github.com/EvolvingLMMs-Lab/lmms-eval.git
# cd lmms-eval && pip install -e ".[video]"

# ── Core inference ────────────────────────────────────────────────────────────
transformers>=4.49.0
accelerate>=0.30.0
huggingface-hub>=0.23.0

# ── Model-specific dependencies ──────────────────────────────────────────────
# InternVL3
einops>=0.7.0
timm>=0.9.0
sentencepiece>=0.1.99

# Qwen2.5-VL
qwen-vl-utils>=0.0.8

# General
protobuf>=4.25.0

# ── Video processing ─────────────────────────────────────────────────────────
av>=12.0.0
decord>=0.6.0
opencv-python>=4.9.0

# ── Quantization (for 72B/78B on limited VRAM) ──────────────────────────────
bitsandbytes>=0.43.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0
tabulate>=0.9.0
